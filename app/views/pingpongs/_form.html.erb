<%= form_for(@pingpong) do |f| %>
  <% if @pingpong.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pingpong.errors.count, "error") %> prohibited this pingpong from being saved:</h2>

      <ul>
      <% @pingpong.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
