<%= form_for(@first) do |f| %>
  <% if @first.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@first.errors.count, "error") %> prohibited this first from being saved:</h2>

      <ul>
      <% @first.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
